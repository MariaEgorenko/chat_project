# Chat Project

Веб-приложение для обмена сообщениями, построенное на базе **FastAPI**. Проект реализует функционал чата с возможностью управления контактами, аутентификацией пользователей и серверным рендерингом страниц с использованием Jinja2.

## 📋 Основной стек технологий

- **Backend:** [FastAPI](https://fastapi.tiangolo.com/)
- **Server:** Uvicorn
- **Database ORM:** SQLAlchemy
- **Validation:** Pydantic
- **Templates:** Jinja2
- **Auth:** OAuth2 с JWT токенами (`python-jose`, `passlib`)

## 📂 Структура проекта

Проект организован по модульному принципу внутри директории `app/`:

```text
chat_project/
├── app/
│   ├── api/          # Роуты и контроллеры API
│   ├── core/         # Конфигурация приложения и настройки безопасности
│   ├── db/           # Модели базы данных и сессия SQLAlchemy
│   ├── services/     # Бизнес-логика (работа с чатами, контактами)
│   ├── static/       # Статические файлы (JS, CSS)
│   ├── templates/    # HTML-шаблоны (Jinja2)
│   ├── __init__.py
│   └── main.py       # Точка входа в приложение
├── .gitignore
└── requirements.txt  # Зависимости проекта
```

## 🚀 Установка и запуск
Предварительные требования
- Python 3.9 или выше
- Установленный Git

### 1. Клонирование репозитория
   
```bash
git clone https://github.com/MariaEgorenko/chat_project.git
cd chat_project
```


### 2. Создание виртуального окружения
Рекомендуется использовать виртуальное окружение, чтобы избежать конфликтов зависимостей.

**Linux/macOS:**
```bash
python3 -m venv venv
source venv/bin/activate
```

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Запуск сервера
Команда выполняется из корня проекта:

```bash
uvicorn app.main:app --reload
```